---
import SanityCode from '@app/components/astro/sanity/code.astro';
import type { CommonSchemaType } from '@app/types/content';

export type SanityCodeType = CommonSchemaType<'code'>;

export interface Props extends SanityCodeType {}

const { ...rest } = Astro.props;
---

<SanityCode code={rest} />
